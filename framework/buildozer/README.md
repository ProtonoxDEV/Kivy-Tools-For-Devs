Protonox Buildozer fork
=======================

Buildozer sources belong here once fetched from upstream `master`. The Protonox patchset will
update defaults for SDK 34, NDK r25c, Java 17, and python-for-android integration. Record the
synced commit in `VERSION.txt` after cloning and stripping `.git` metadata.
