Protonox windowser layer
========================

Provides native-facing scaffolding for permissions, storage, notifications, and background services.
Java stubs mirror the expected Android entry points while Python modules expose minimal helpers that
can be wired into Pyjnius once upstream dependencies are synchronized.
