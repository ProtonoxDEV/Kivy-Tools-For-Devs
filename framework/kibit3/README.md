kibit3 emoji/text engine (skeleton)
===================================

This lightweight implementation provides the hooks Protonox Kivy will use for emoji parsing and
text shaping. It can be expanded with platform-specific renderers once upstream Kivy is synced.
